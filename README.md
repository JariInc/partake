# partake aka iracer.info #

## Requirements ##

* Python 3.4
* PostgreSQL 9.x

see requirements.txt for the required python packages or install them with pip -r requirements.txt. Use of Virtualenv is highly encouraged.